\documentclass{gwiki}
\usepackage{gwiki-links}
\usepackage{tz}

\Title{tikz test 17 intersections}
\Tags{tikz, visualization, test}

\begin{document}
\NoteHeader

Path intersections and clipping. \allformats{tikz test 17 intersections}

Navigation: \wref{tikz test 16 3d effects} (previous) | \wref{tikz test 18 matrix} (next)

\section*{Demonstrations}

\[
\tz{
  \begin{scope}
    \clip (0,0) circle (1cm);
    \fill[colA!50] (1,0) circle (1cm);
  \end{scope}
  \draw (0,0) circle (1cm);
  \draw (1,0) circle (1cm);
}
\]

\SeeAlso{tikz test 01 patterns}

\end{document}
