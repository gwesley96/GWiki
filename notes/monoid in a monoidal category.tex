\documentclass{gwiki}
\usepackage{gwiki-links}

\Title{monoid in a monoidal category}
\Tags{}

\begin{document}
\NoteHeader

A \textbf{monoid object} \textbf{TODO}

In the $\mathsf{Vec}$-enriched case, a monoid object is called an \textbf{algebra object}.

% #unfinished
A unital algebra object $A$ in a \wref{linear monoidal category} $\mathcal{C}$ is called \textbf{connected} if $\dim\mathcal C(1_{\mathcal{C}}\to A)=1$.

\textbf{\wref[Definition 2.36]{Rowett19MorrisonLadderCategories.pdf#page=30&selection=221,0,221,14}.} 

See also:
\begin{itemize}
\item \wref{monad in a 2-category}
\end{itemize}

\end{document}
